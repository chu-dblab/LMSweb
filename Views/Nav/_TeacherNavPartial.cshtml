@model  LMSweb.ViewModel.NavPartial.TeacherNavViewModel
@{
    var CourseID = Model.CourseID;
}

@AllHelper.SetActiveNavData(Url.Action("Index", "Missions", new { CID = CourseID }), "任務管理")
@AllHelper.SetUnActiveNavData(Url.Action("StudentManagement", "Course", new { CID = CourseID }), "學生名單")
@AllHelper.SetUnActiveNavData(Url.Action("TeacherJourney", "LearningBehaviors", new { CID = CourseID }), "學習表現")
@AllHelper.SetUnActiveNavData(Url.Action("TeacherEvalutionJourney", "LearningBehaviors", new { CID = CourseID }), "學習表現(互評版)")
@AllHelper.SetUnActiveNavData(Url.Action("Index", "LearningBehaviors", new { SearchID = CourseID }), "新學習表現")

